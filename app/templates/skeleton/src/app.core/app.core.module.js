(function(angular) {
    'use strict';

    /**
     * add more angular and third party modules here that should be available all over the app
     */
    angular.module('app.core', [
        'ui.router',
        'ngAnimate'
    ]);
    angular.module('app.core').config(function($stateProvider) {

        /* Add New States Above */

    });

})(angular);
