(function(angular) {
    'use strict';

    angular.module('<%= _.camelize(name) %>', ['app.core']);
    angular.module('<%= _.camelize(name) %>').config(function($stateProvider) {

        /* Add New States Above */

    });

})(angular);
